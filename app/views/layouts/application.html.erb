<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Git Video</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <link href='http://fonts.googleapis.com/css?family=Indie+Flower' rel='stylesheet' type='text/css'>
    <link href='http://fonts.googleapis.com/css?family=Stalemate' rel='stylesheet' type='text/css'>

    <!-- Le styles -->
    <%= stylesheet_link_tag "bootstrap" %>
    <style type="text/css">
      body {
        padding-top: 20px;
        padding-bottom: 40px;
      }

      /* Custom container */
      .container-narrow {
        margin: 0 auto;
        max-width: 700px;
      }
      .container-narrow > hr {
        margin: 30px 0;
      }

      /* Main marketing message and sign up button */
      .jumbotron {
        margin: 60px 0;
        text-align: center;
      }
      .jumbotron h1 {
        font-size: 72px;
        line-height: 1;
      }
      .jumbotron .btn {
        font-size: 21px;
        padding: 14px 24px;
      }

      /* Supporting marketing content */
      .marketing {
        margin: 60px 0;
      }
      .marketing p + h4 {
        margin-top: 28px;
      }
      .muted {
        color: #999999;
      }
      .hand {
        font-family: 'Indie Flower', cursive;
        font-size:16px;
        color: #009900;
      }
      .table th,
      .table td {
        vertical-align: middle; 
      }
      .logo {
        font-family: 'Stalemate', cursive;
        text-decoration:none;
        text-shadow: 1px 1px #0066aa;
        font-size:56px;
      }
    </style>
    <%= stylesheet_link_tag "bootstrap-responsive" %>

    <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
  </head>

  <body>

    <div class="container-narrow">

      <% if current_user %>
      <div class="masthead">
        <ul class="nav nav-pills pull-right">
          <img style="float:left;width:35px;margin-right:15px;" class="img-circle" src="<%= @user.image %>"/>
          <li><a href="/">Home</a></li>
          <li><a href="/repos">Repos</a></li>
          <li><%= link_to "Sign Out", signout_path %><li>
        </ul>
        <h3 class="muted"><a style="color:#000;text-decoration:none;" class="logo" href="/">Git Video</a></h3>
      </div>

      <hr>
      <% end %>

      <%= yield %>

      <hr>
      <div style="text-align:center;" class="footer">
        <p><a href="http://gaidi.ca">gaidi.ca</a></p>
      </div>

    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "bootstrap.min" %>

  </body>
</html>
